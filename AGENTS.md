# AGENTS.md

Agent instructions for this repository live in **[CLAUDE.md](CLAUDE.md)**.

That file is the single source of truth — stack, commands, layout, engineering
principles, and the branch and pull-request rules that CI enforces. This file
exists so that agents looking for the `AGENTS.md` convention find their way there,
and is kept as a pointer rather than a copy so the two cannot drift apart.
